namespace ShapeGenerator
{
    public enum Shape
    {
        Circle, //working
        Cylinder, // circle with -F (fill)
        Sphere,//ball
        Square,
        Box,
        Line,
        Triangle,
        Piramid,
        Rectangle,

    }
}